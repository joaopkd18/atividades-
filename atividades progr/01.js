function calcuarAreRetangulo() {
    let  base = Number(prompt ("Digite a base : "))
let altura = Number(prompt("Digite a altura:"))
    let area = base*altura
    return alert ("A area do retangulo e: "+ area ) 
}
calcuarAreRetangulo()
